module app;

void CreateZombies() {
    for (i32 i = 0; i < 10000; ++i) {
        Zombie z = create Zombie;
        z.name = "Marcus " + i;
    }
}

void Greet(Zombie z) {
    z.SayHello();
}

schedule {
    run CreateZombies;
    run Greet;
}
