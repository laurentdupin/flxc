module app;

void CreateZombies() {

    Array<string> names = ["Marcus", "Louis"];

    for (i32 i = 0; i < names.length(); ++i){
        Zombie z = create Zombie;
        z.name = names[i];
    }
}

void Greet(Zombie z) {
    z.SayHello();
}

schedule {
    run CreateZombies;
    run Greet;
}
